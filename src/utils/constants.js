export const RESPONSE = Object.freeze({
  PENDING: 'pending',
  RESOLVED: 'resolved',
  REJECTED: 'rejected',
})

export const FILTER = Object.freeze({
  FAVORITES: 'favourites',
  CART_ITEMS: 'cart',
  DEFAULT: 'promos',
})
